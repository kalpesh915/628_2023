#include<stdio.h>
#include "math2.h"

/**
    use <> when you store header files in c:\tc\include
    use "" when you store header files in bin folder or same folder where you
    save programs
*/

void main(){
    printf("\n sum is %d", sum(111, 22));
    printf("\n sub is %d", sub(121, 22));
    printf("\n mul is %d", mul(131, 22));
    printf("\n div is %f", div(141, 22));
    printf("\n mod is %d", mod(131, 22));
    printf("\n max is %d", max1(141, 22));
    printf("\n min is %d", min1(115, 22));
    printf("\n square %d", square(5));
    printf("\n cube %d", cube(5));
}

